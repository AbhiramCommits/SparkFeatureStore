# Shared dependency lock for all Docker images.
#
# Single source of truth for image builds; mirrors the pins in
# pyproject.toml / uv.lock. Dockerfile.spark installs the "spark-driver"
# section; Dockerfile.train installs the "train" section (torch from the
# CPU wheel index to keep the image size down).
#
# spark-driver
pyyaml==6.0.2
psycopg[binary]==3.2.3
# train
pandas==2.2.3
numpy==2.2.1
scikit-learn==1.6.1
joblib==1.4.2
pyarrow==18.1.0
s3fs==2024.10.0
fsspec==2024.10.0
torch==2.5.1
requests==2.32.3
pyyaml==6.0.2
psycopg[binary]==3.2.3
